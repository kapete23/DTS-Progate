// Define the width variable, then assign 7 to it
var width = 7;

// Define the height variable, then assign 9 to it
var height = 9;

// Define the area variable, then assign the result of width * height to it
var area = width * height;

// Print the area variable
console.log (area);
